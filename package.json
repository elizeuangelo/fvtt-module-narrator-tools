{
	"name": "narrator-tools",
	"version": "1.0.1",
	"description": "Special tools to increase the immersion in your roleplaying game.",
	"private": true,
	"type": "module",
	"main": "main.ts",
	"author": "elizeuangelo",
	"license": "ISC",
	"scripts": {
		"update-patch": "npm version patch && git push && git push --tags",
		"update-minor": "npm version minor && git push && git push --tags",
		"update-major": "npm version major && git push && git push --tags",
		"foundry:release": "node ./build.mjs"
	}
}
